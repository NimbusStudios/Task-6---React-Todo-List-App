// App Component
1. Import necessary libraries and components
2. Define the App function
   3. Set up the Router
   4. Define Routes
      5. Redirect root path to login
      6. Route for Registration page
      7. Route for Login page
      8. Route for Home page with ProtectedRoute
      9. Route for Profile page with ProtectedRoute
10. Export the App component

// ProtectedRoute Component
1. Import necessary libraries
2. Define the ProtectedRoute function
   3. Check if user is authenticated by checking localStorage
   4. If authenticated, render children
   5. If not authenticated, redirect to login
6. Export the ProtectedRoute component

// Registration Component
1. Import necessary libraries
2. Define the Registration function
   3. Initialize state for form data
   4. Define the handleRegister function
      5. Prevent default form submission
      6. Save user data to localStorage
      7. Redirect to login page
   8. Render the registration form
9. Export the Registration component

// Login Component
1. Import necessary libraries
2. Define the Login function
   3. Initialize state for form data
   4. Define the handleLogin function
      5. Prevent default form submission
      6. Retrieve user data from localStorage
      7. If credentials match, navigate to home page
      8. If credentials do not match, show alert
   9. Render the login form
10. Export the Login component

// Home Component
1. Import necessary libraries and components
2. Define the Home function
   3. Initialize state for tasks
   4. Use effect to load tasks from localStorage
   5. Define the addTask function
      6. Add new task to state and localStorage
   7. Define the updateTask function
      8. Update task in state and localStorage
   9. Define the deleteTask function
      10. Delete task from state and localStorage
   11. Render AddTask and TodoList components
12. Export the Home component

// AddTask Component
1. Import necessary libraries
2. Define the AddTask function
   3. Initialize state for task
   4. Define the handleAddTask function
      5. Prevent default form submission
      6. Add task and reset form
   7. Render the add task form
8. Export the AddTask component

// TodoList Component
1. Import necessary libraries and components
2. Define the TodoList function
   3. Render TodoItem for each task
4. Export the TodoList component

// TodoItem Component
1. Import necessary libraries
2. Define the TodoItem function
   3. Render task details
   4. Render delete button
   5. Render update button to toggle task status
6. Export the TodoItem component

// Profile Component
1. Import necessary libraries
2. Define the Profile function
   3. Initialize state for user data
   4. Use effect to load user data from localStorage
   5. Define the handleUpdate function
      6. Prevent default form submission
      7. Save updated user data to localStorage
   8. Render the profile form
9. Export the Profile component
